import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const Sepetim = () => {
  return (
    <SafeAreaView>
      <Text>Sepetim Page</Text>
    </SafeAreaView>
  );
};

export default Sepetim;
